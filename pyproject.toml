[project]
name = "llm_playground"
version = "0.1.0"
description = "LLM tool usage demo"
requires-python = ">=3.9"
dependencies = [
    "openai",
    "httpx",
    "numpy>=2.0.2",
    "pandas>=2.3.3",
]

[tool.uv]
package = false

